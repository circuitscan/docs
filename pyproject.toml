[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "circuitscan"
authors = [{name = "numtel", email = "ben@latenightsketches.com"}]
dynamic = ["version", "description"]
